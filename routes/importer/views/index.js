const { tutorial } = require('./tutorial');
const { list } = require('./list');
const { yiff } = require('./yiff');
const { ok } = require('./ok');

module.exports = {
  tutorial,
  list,
  yiff,
  ok
};
